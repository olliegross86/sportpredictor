@model SportTypeModel
@{
    ViewData["Title"] = "Home Page";
}

    <div class="text-center welcome">
        <h1 class="display-4">Welcome</h1>
        <p>@ViewBag.Description</p>

        @*<select id="dropdown">
            <option value="Sport1">@Model.SportType</option>

        </select>

        <select id="teamsdropdown">

            @for (var i = 0; i < Model.Teams.Count(); i++)

            {

                <option value="Team1">@Model.Teams[i]</option>

            }

        </select>

        <select id="resultsdropdown">
            @for (var i = 0; i < Model.Scores.Count(); i++)

            {

                <option value="Result1">@Model.Scores[i]</option>

            }

        </select>


    </div>*@

    @*<div>
        <table  align="right" class="container-fluid" border="0" cellspacing="5" cellpadding="25">
            <tbody>
                <tr>
                    @for (int i = 0; i < 6; i++)
                    {
                       
                        <td>
                            <img width="150" height="150" src=@Model.Logos[i] />
                        </td>


                
                    }
                </tr>

                <tr>
                    @for (int i = 7; i < Model.Logos.Count; i++)
                    {
                        <td>
                            <img width="150" height="150" src=@Model.Logos[i] />
                        </td>

                    }
                </tr>

            </tbody>
        </table>
    </div>*@
